# pylint: disable=missing-module-docstring,missing-class-docstring,disable=too-few-public-methods,invalid-name
class Driver:
    def __init__(self, color, position, speed, turnNum):
        self.color = color
        self.position = position
        self.speed = speed
        self.turnNum = turnNum
